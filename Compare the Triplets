
vector<int> compareTriplets(vector<int> a, vector<int> b) {
    int alice=0;
    int bob=0;
    for(int i=0;i<a.size();i++){
        if(a[i]>b[i])alice++;
        else if(a[i]<b[i])bob++;
        else continue;
    }
    return {alice,bob};
}
